spring:
  server:
    port: 8080

  datasource:
    url: jdbc:mysql://localhost:3306/wwld
    username: root
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
        hibernate:
            dialect: org.hibernate.dialect.MySQL8Dialect
